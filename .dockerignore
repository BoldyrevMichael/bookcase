# В образ едет только собранный архив, который передаётся явным аргументом.
# Всё остальное — исходники, кэши сборки, отчёты — увеличивало бы объём передаваемого
# демону контекста на сотни мегабайт без всякой пользы.
*
!services/*/build/libs/*.jar
!web
