package com.bookcase.catalog.dto;

/**
 * Где искать.
 *
 * <p>Значение пока одно, и это сделано намеренно: поиск по содержимому книг — возможное развитие, и
 * когда оно появится, запросы существующих клиентов останутся верными. Добавить параметр потом, не
 * меняя договорённости с клиентами, было бы уже нельзя.
 */
public enum SearchScope {

    /** По названию, авторам, серии и издательству. */
    METADATA
}
