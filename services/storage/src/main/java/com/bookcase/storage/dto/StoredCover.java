package com.bookcase.storage.dto;

/**
 * Обложка книги.
 *
 * <p>Владельца у неё нет, и это осознанно: обложку уже опубликовал внешний справочник, она
 * одинакова у всех, у кого есть эта книга, и личными данными не является. Поэтому она хранится
 * одним объектом на всех и отдаётся без пропуска — в отличие от самой книги.
 *
 * @param sha256 хэш содержимого, он же имя объекта в хранилище и адрес выдачи
 * @param contentType тип картинки
 * @param sizeBytes размер
 */
public record StoredCover(String sha256, String contentType, long sizeBytes) {}
