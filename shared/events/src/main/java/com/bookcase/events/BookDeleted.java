package com.bookcase.events;

import java.time.Instant;
import java.util.UUID;

/**
 * Книга убрана из библиотеки.
 *
 * <p>Событие нужно тем, кто завёл у себя что-то, привязанное к этой книге, и сам об удалении не
 * узнает. Файл и обложку каталог отпускает сразу, своими руками, — а вот уточнитель держит задачу
 * на эту книгу, и без такого извещения она дождётся своей очереди и потратит запрос к справочнику
 * на книгу, которой больше нет.
 *
 * <p>Второй случай тот же, но наоборот: если удаление случилось, пока справочник думал, обложка
 * приедет к несуществующей карточке. Отпустить её должен тот, кто положил.
 *
 * @param eventId идентификатор события
 * @param bookId карточка, которой не стало
 * @param ownerId владелец
 * @param occurredAt когда книгу удалили
 */
public record BookDeleted(UUID eventId, UUID bookId, String ownerId, Instant occurredAt) {}
